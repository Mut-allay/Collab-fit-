Before it works — 3 things to set up
1. Google Cloud Console

Create an OAuth2 credential (Web application)
Add authorized redirect URI: https://<your-render-app>.onrender.com/api/auth/google/callback
Add http://localhost:3000/api/auth/google/callback for local dev